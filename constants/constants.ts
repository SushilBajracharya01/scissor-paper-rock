export const initialScore = {
  winCount: 0,
  loseCount: 0,
  drawCount: 0,
};

export enum EOptions {
  ROCK = 'ROCK',
  PAPER = 'PAPER',
  SCISSOR = 'SCISSOR',
}

export enum ERESULT {
  DRAW = 'DRAW',
  WIN = 'WIN',
  LOSE = 'LOSE',
}
